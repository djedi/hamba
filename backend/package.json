{
  "name": "hamba-backend",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../.env --hot run src/index.ts",
    "start": "bun --env-file=../.env run src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "migrate": "bun run src/db/migrate.ts",
    "migrate:status": "bun run src/db/migrate.ts status",
    "migrate:rollback": "bun run src/db/migrate.ts rollback",
    "migrate:create": "bun run src/db/migrate.ts create"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/mailparser": "^3.4.6",
    "@types/nodemailer": "^7.0.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "@elysiajs/cors": "^1.4.0",
    "@elysiajs/swagger": "^1.3.1",
    "elysia": "^1.4.19",
    "imapflow": "^1.2.0",
    "mailparser": "^3.9.1",
    "nodemailer": "^7.0.11"
  }
}
